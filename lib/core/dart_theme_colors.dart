import 'package:flutter/material.dart';

class AppDarkThemeColors {
  // default theme
  static const primaryColor = Color(0xFF4CAF50); // green
  static const primaryColorLight = Color(0xFF81C784);
  static const backgroundColor = Color(0xFF424242);
  static const selectedItemColor = Color(0xFF546DFE);
  static const unselectedItemCOlor = Color(0xFF000000);
}
